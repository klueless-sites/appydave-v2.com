---
import Prose from '../Prose.astro'

// {(title || section) && (
//     <header class="mb-9 space-y-1">
//       {section && (
//         <p class="font-display text-sm font-medium text-sky-500">
//           {section.title}
//         </p>
//       )}
//       {title && (
//         <h1 class="font-display text-3xl tracking-tight text-slate-900 dark:text-white">
//           {title}
//         </h1>
//       )}
//     </header>
//   )}

---
<article>
  <header class="mb-9 space-y-1">
    <p class="font-display text-sm font-medium text-sky-500">
      <!-- Need to get this from the navigation link title -->
      Section Title
    </p>
    <h1 class="font-display text-3xl tracking-tight text-slate-900 dark:text-white">
      <!-- Need to get this from the page title -->
      This Title
    </h1>
  </header>
  <Prose>
    <slot />
  </Prose>
</article>
